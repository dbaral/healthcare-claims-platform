server:
  port: 8094

spring:
  application:
    name: fraud-check-service
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    consumer:
      group-id: fraud-check-service
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: com.debish.health.fraudcheck.application.event
        spring.json.value.default.type: com.debish.health.fraudcheck.application.event.MemberEligibleEvent
        spring.json.use.type.headers: false
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      properties:
        spring.json.add.type.headers: false

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus

app:
  kafka:
    topics:
      member-eligible: member-eligible
      claim-fraud-cleared: claim-fraud-cleared
      claim-fraud-flagged: claim-fraud-flagged
