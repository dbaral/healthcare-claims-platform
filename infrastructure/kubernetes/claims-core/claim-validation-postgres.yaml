apiVersion: apps/v1
kind: Deployment
metadata:
  name: claim-validation-postgres
  namespace: healthcare-claims
spec:
  replicas: 1
  selector:
    matchLabels:
      app: claim-validation-postgres
  template:
    metadata:
      labels:
        app: claim-validation-postgres
    spec:
      containers:
        - name: claim-validation-postgres
          image: postgres:17
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              valueFrom:
                configMapKeyRef:
                  name: claims-platform-config
                  key: CLAIM_VALIDATION_DB_NAME
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: claims-platform-secret
                  key: CLAIM_VALIDATION_DB_USERNAME
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: claims-platform-secret
                  key: CLAIM_VALIDATION_DB_PASSWORD
---
apiVersion: v1
kind: Service
metadata:
  name: claim-validation-postgres
  namespace: healthcare-claims
spec:
  selector:
    app: claim-validation-postgres
  ports:
    - port: 5432
      targetPort: 5432
